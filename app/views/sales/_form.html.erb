<%= form_with(model: sale, local: true) do |form| %>
  <% if sale.errors.any? %>
    <div id="error_explanation" class="text-danger">
      <h2><%= pluralize(sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :client_id, class:"form-label" %>
    <%= form.collection_select( :client_id, @client_name, :id, :name, { include_blank: "Select Client"}, class:"form-select") %>
  </div>

  <div class="form-group">
    <%= form.label :product_id, class:"form-label" %>
    <%= form.collection_select( :product_id, @product_name, :id, :name, { include_blank: "Select Product"}, class:"form-select" )%>
    
  </div>
  <hr>
  <div class="actions">
    <%= form.submit nil, class:"btn btn-info"  %>
  </div>
<% end %>
